# 需求质量检查清单: 视频处理功能 (013)

## 需求完整性

- [x] 所有用户故事具有明确的 Given/When/Then 结构
- [x] 功能需求覆盖所有用户故事中描述的场景
- [x] 关键实体定义完整，包含必要字段
- [x] 成功标准可量化、可验证
- [x] 假设条件已明确记录
- [x] 依赖外部工具（ffmpeg/ffprobe）的行为已定义降级策略

## 技术可行性

- [x] 缩略图生成通过 ffmpeg 关键帧提取实现，技术成熟
- [x] 元数据提取通过 ffprobe JSON 输出解析实现，覆盖所有目标字段
- [x] 批量编辑为纯内存操作 + JSON 写入，无性能瓶颈
- [x] 批量重命名使用标准文件系统操作（`std::fs::rename`），安全可靠
- [x] 格式转换通过 ffmpeg 子进程实现，支持进度回调

## 架构一致性

- [x] 视频处理操作通过独立的 `video_processing.rs` 服务实现
- [x] 新模型（VideoMetadata、ConversionTask 等）遵循项目命名约定
- [x] ffmpeg/ffprobe 调用封装在服务层，Commands 层不直接执行外部命令
- [x] 缩略图缓存在 `~/.yt-dlp-sub-gui/thumbnails/`，符合现有数据目录结构
- [x] 转换进度通过 Tauri 事件推送到前端

## 边界条件

- [x] ffmpeg/ffprobe 未安装时的用户引导和错误提示已定义
- [x] 视频文件损坏导致元数据提取失败时的优雅降级
- [x] 无视频流的纯音频文件缩略图生成策略（使用占位图）
- [x] 批量重命名中特殊字符（如 `/`, `:`, `\0`）的过滤处理
- [x] 格式转换中源文件和目标文件同路径的冲突处理
- [x] 格式转换取消时正确终止子进程（kill + 清理部分文件）

## 安全性

- [x] ffmpeg 命令行参数通过数组构建，避免注入
- [x] 重命名操作限制在下载目录范围内（防止路径遍历攻击）
- [x] 缩略图缓存文件大小限制（防止恶意视频文件生成超大缩略图）
- [x] 用户提供的文件名模板中的变量经过严格过滤

## 依赖与集成

- [x] ffmpeg/ffprobe 路径可通过设置配置（支持自定义安装位置）
- [x] 缩略图生成与现有 download_complete 事件集成
- [x] 元数据提取结果复用现有 DownloadRecord 模型中的字段
- [x] 格式转换与现有文件管理不冲突（保留原始文件）

## 测试策略

- [x] 重命名模板变量替换逻辑可单元测试
- [x] 文件名合法性检查可单元测试
- [x] 元数据解析（ffprobe JSON 反序列化）可单元测试
- [x] 缩略图生成和格式转换需要集成测试（依赖 ffmpeg）
- [x] 批量编辑撤销/恢复逻辑可单元测试

## 状态

所有检查项已通过。规范已准备好进入实施阶段。
